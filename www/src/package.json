{
	"scripts": {
		"build": "npm --prefix coding_challenges run build && npm --prefix games run build && npm --prefix microsim run build",
		"clean": "npm --prefix coding_challenges run clean && npm --prefix games run clean && npm --prefix microsim run clean"
	}
}