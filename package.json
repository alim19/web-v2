{
	"scripts": {
		"install": "npm --prefix server install && npm --prefix web-builder install",
		"build": "npm --prefix server run build && npm --prefix web-builder run build && npm --prefix www run build",
		"clean": "npm --prefix server run clean && npm --prefix web-builder run clean && npm --prefix www run clean",
		"test": "npm --prefix server run test",
		"start": "npm --prefix server run start"
	}
}
